Consider any Dyck path $D$ in the grid $\mathbb{Z}^2$ that stays above the diagonal $(1,0)$ to $(n+1,n)$. Take $P$ as the Minkowski sum of all simplices $\Delta_{ij}={\rm Newt}(1+x_i+x_ix_{i+1}+\ldots+x_i\cdots x_j)$ for all $(i,j)$ on or below $D$. How many facets does $P$ have?
