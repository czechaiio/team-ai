Consider a 20 by 20 square grid with lower left corner at (0,0), upper right at (20,20). What is the number of ways to draw 21 monotone grid paths (each having only E or N steps) through the grid which connect (0,0),..,(20,0) with (20,0).....,(20,20) so that no two paths have overlapping steps, endpoints and so that two paths may cross over at a point but not just touch, the path starting at (20,0) ends at (20,12) and the path starting at (10,0) ends at (20,0)?
